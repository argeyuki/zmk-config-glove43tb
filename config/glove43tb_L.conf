# ロータリーエンコーダー（左手側のダイヤル）
#
# EC11 ドライバ自体は alps,ec11 のノードが有効なら自動で入る（Kconfig 上 default y）。
# ただしトリガーの既定は EC11_TRIGGER_NONE で、回転を検知する仕掛けが動かない。
# そのため overlay で status = "okay" にしてあっても、回しても何も起きなかった。
CONFIG_EC11_TRIGGER_GLOBAL_THREAD=y
